# Application settings
# Managed via /api/v1/config/settings API and Config UI

ui:
  language: en

search:
  engine: duckduckgo
  max_results: 5

privacy:
  strict_mode: false
  redact_traces: true
  retention_days: 90
